<%= javascript_include_tag 'style' %>
<div class="row" style="background-color: #e8e8e8">
  <div class="">
    <nav style="margin-bottom: 20px;background-color: #640000">
      <div class="nav-wrapper">
        <a class="brand-logo right" style="padding-right: 20px">Grade 11</a>
        <a href="#" data-activates="slide-out" class="button-collapse">
          <i class="fa fa-bars fa-2x" aria-hidden="true"></i>
        </a>
      </div>
    </nav>

      <div class="col s12 m12 l9 right">

        <div class="student-list">
          <h4 class="center">HERMES</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11A.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11A.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>
        

        <div class="student-list">
          <h4 class="center">ATHENA</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11B.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11B.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">HERA</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11C.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11C.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">DEMETER</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11D.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11D.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">CERRIDWEN</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11E.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11E.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">HERCULES</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11F.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11F.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">IRIS</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11G.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11G.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">POSEIDON</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11H.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11H.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">APOLLO</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11I.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11I.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">HESPHAESTUS</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11J.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11J.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

        <div class="student-list">
          <h4 class="center">VENUS</h4>
          <table class="striped responsive-table">
            <thead>
              <tr>
                  <th>LRN</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Email</th>
                  <th>Gender</th>
                  <th>Registered at</th>
              </tr>
            </thead>

            <tbody>
              <% if @students11K.empty? %>
                <tr>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                  <td>No data yet</td>
                </tr>
              <% else %>
                <% @students11K.each do |student| %>
                  <tr>
                    <td><%= student.lrn %></td>
                    <td><%= link_to student.firstname, student_path(student.id) %></td>
                    <td><%= student.lastname %></td>
                    <td><%= student.email %></td>
                    <td><%= student.gender %></td>
                    <td><%= student.created_at.to_s(:short) %></td>
                  </tr>
                <% end %> 
              <% end %>  
            </tbody>
          </table>  
        </div>

      </div>

  </div>
  <div class="col s12 ">
      <%= render 'partials/sidebar' %>
  </div>
</div>
  
<style type="text/css">
  body {
    background-color: #e8e8e8;
  }
  .modal.bottom-sheet {
    max-height: 100%!important;
  }

  .modal .modal-footer {
    height: 80px!important;
  }

  .collapsible-body {
    padding: 3.5px!important;
  }

  .student-list {
    border:1px solid #9e9e9e;
    padding:20px;
    border-radius: 20px;
    margin-top: 20px;
  }

</style>
